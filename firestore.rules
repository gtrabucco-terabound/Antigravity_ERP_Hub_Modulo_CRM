rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Reglas básicas: el usuario debe estar autenticado
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
